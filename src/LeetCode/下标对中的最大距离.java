package LeetCode;

public class 下标对中的最大距离 {
    // 二分
    public int maxDistance(int[] nums1, int[] nums2) {
        int ans = 0;
        for (int i = 0; i < nums1.length; i++) {
            // i <= j && nums[i] <= nums[j]
        }


        return ans;
    }
}
